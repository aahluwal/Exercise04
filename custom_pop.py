def custom_equality(some_list, another_list):
    """custom_equality(some_list, another_list) imitates
    (some_list == another_list)
    """
    if type(some_list) == type(another_list) and custom_len(some_list) = custom_len(another_list):
    	return True
    else:
    	return False
    	